object Translation {
/*
	// What import needed for AST?
	//import whiley.WhileyISyntax.Program

	// traverse tree recursively
	// input node in AST of Program, initially first node in Program
	// output C file or String to be written to a C file?
	def traverse(Node n) {
		// Pattern match for identifying nodes in the AST.
		node match {
			case DeclAsgn => 
				traverse(left);

		}

		// Call functions for keywords and recursively call traverse on the required child nodes.

	}

	// input node in AST p
	// return String constructed by traversing nodes subtrees
	// DeclAsgn of the form: Type Loc "=" Exp
	def convertDeclAsgn(Node r) : String {
		val declAsgn = traverse(left) + traverse(middle) + "=" + traverse(right);
	}

*/
	
	def traverse()
}